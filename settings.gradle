pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'LanMultiplayerSdk'
include ':app'
project(':app').projectDir = file('app')